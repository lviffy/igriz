# GROQ API keys — add multiple keys for automatic fallback on rate limits
# The system will rotate to the next key when a 429 rate-limit is hit.
GROQ_API_KEY1=
GROQ_API_KEY2=
GROQ_API_KEY3=

# Legacy single-key (used if no numbered keys are set)
# GROQ_API_KEY=

PRIVATE_KEY_QUAI=
PRIVATE_KEY_DATAHAVEN=